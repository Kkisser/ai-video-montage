# Правила монтажных паспортов сцен

Формат, по которому чат со сценарием описывает каждую сцену для автомонтажа.
На каждую сцену — один блок в самом её конце, в порядке воспроизведения.

## Блок сцены

```
[MONTAGE]
file: <отличительное слово из имени клипа: cliff, talks, brushing, finale…>
text: <точная реплика сцены — только произносимые слова>
trim: <tight | normal | loose | none>
hook: <текст плашки на весь кадр — ТОЛЬКО если в сцене нет реплики; иначе строку убери>
keep: <«эмоция» — где паузу не резать; или «нет»>
[/MONTAGE]
```

## Поля

| Поле | Что писать |
|---|---|
| **file** | Короткое слово, по которому найдётся файл клипа (напр. `brushing`). |
| **text** | Только звучащие слова. Числа — словами в правильной форме («двадцать процентов», «две минуты», «шестьдесят дней»). Коды моделей — латиницей и цифрами (`RL 066`). Пунктуацию можно не ставить. |
| **trim** | `tight` — быстрая речь, режь плотно · `normal` — обычно · `loose` — оставь воздух/комедийную паузу · `none` — не резать вовсе (песня, проигрыш). |
| **hook** | Только для сцены без реплики (общий план-завязка, титр). Иначе строку убрать. |
| **keep** | `эмоция` — есть пауза по задумке (драм-бит, реакция), не резать; `нет` — резать можно свободно. |

## Условные обозначения внутри text (по смыслу, необязательно)

| Символ | Значение |
|---|---|
| `*слово*` | Акцент — выделить слово (крупнее/цветом). |
| `~` | Держать паузу здесь, не сжимать тишину (между словами). |
| `//` | Принудительный разрыв — следующее слово начинает новую плашку. |

Пример text: `ставь лайк если ты *за звезду* ~ // а если за обеих пиши сикс севен`

## Что уже работает, что задел на будущее

| Флаг | Статус |
|---|---|
| `text` (сабы, числа словами, RL 066 латиницей) | ✅ работает |
| `trim` (tight/normal/loose/none) | ✅ работает |
| `hook` (плашка для сцены без реплики) | ✅ работает |
| `keep` (не резать паузу-эмоцию) | ⏳ следующий шаг |
| `*акцент*`, `~` (пауза), `//` (разрыв) | ⏳ следующий шаг |

Неизвестные флаги парсер пропускает, символы из text снимаются вместе с пунктуацией —
поэтому паспорта можно писать «на вырост» уже сейчас, лишнее не ломает монтаж.

## Как это идёт в монтаж

1. Блоки `[MONTAGE]` → сохранить в `in/scenes.txt`.
2. Клипы — в `in/` (имена любые, лишь бы `file:` совпадал по слову).
3. `./venv/bin/python make_clips.py` → `clips.json` (+ пары клип↔сцена для проверки).
4. `./venv/bin/python montage.py` → готовый ролик `out/final.mp4`.
